import R from 'ramda'

const getPhoneById = (state, id) => R.prop(id, state.phones)// find data by id

export const getPhones = state => { // find data in state
  const phones = R.map(id => getPhoneById(state, id), state.phonesPage.ids)
  return phones
}